# Copy this file to .env and fill in your values

# PostgreSQL connection string (required)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/dosewise

# Session secret - required. Generate a long random string, never reuse a known value.
SESSION_SECRET=

# Local server port for the backend API
PORT=8081

# Comma-separated list of allowed browser origins (CORS/CSRF allowlist)
ALLOWED_ORIGINS=http://localhost:5173

# Administrator account created at startup / by the seed script (required)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=

# Node environment
NODE_ENV=development
